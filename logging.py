import logging

logging.basicConfig(level=logging.DEBUG, format='%(asctime)s - %(levelname)s - %(messages)s')


def factorial(n):
    for i in range(n + 1):
        total *= i
        return total


print(factorial(5))
